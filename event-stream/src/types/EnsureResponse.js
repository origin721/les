// @ts-check

/**
 * @typedef {import('./EnsureResponseReadonly')} EnsureResponseReadonly
 */
/**
 * @typedef {import('../http_server/shared_service/server_side_event_connection/add.js/types/SendJsonDtoParams')} SendJsonDtoParams
 */

/**
 * @typedef {Record<string, EnsureResponseReadonly>} EnsureResponsesss
 */
/**
 * @typedef {Object} EnsureResponse 
 * @prop {string} request_id  
 * @prop {SendJsonDtoParams} send_params
 * @prop {Date} date_created
 * @prop {number} active_users
 */

/**
 * @type {EnsureResponse}
 */
const EnsureResponse;

module.exports = EnsureResponse;