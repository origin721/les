# 📋 Changelog

Все важные изменения в проекте документируются в этом файле.

## [Завершено] - 2025-01-08

### 🔧 Исправление счетчика активных вкладок в SharedWorker

**Статус:** ✅ Полностью исправлено

**🎯 Проблема**: Счетчик активных вкладок в настройках (/settings) показывал неправильное количество:
- При открытии 3 вкладок счетчик показывал только "● 1 АКТИВНЫХ" на всех вкладках
- При обновлении страницы количество увеличивалось вместо корректного подсчета
- Счетчик не синхронизировался между вкладками

#### ✅ Исправленные проблемы

##### 🚨 **Критическая ошибка в логике очистки портов**
- **Проблема**: `cleanupDeadPorts()` вызывался при каждом новом подключении в `onconnect`
- **Следствие**: Новые подключения немедленно удаляли существующие порты из `activeTabs` Set
- **Исправление**: Убран вызов `cleanupDeadPorts()` из `onconnect` события

##### ⚠️ **Агрессивные обработчики закрытия страницы**
- **Проблема**: Обработчики `visibilitychange` и `unload` отправляли `disconnect` слишком часто
- **Следствие**: Активные вкладки помечались как закрытые при скрытии страницы
- **Исправление**: 
  - Удален обработчик `visibilitychange` 
  - Удален дублирующий обработчик `unload`
  - Оставлен только `beforeunload` для реального закрытия

#### 🔧 Измененные файлы

1. **`src/processes/shared_worker/process/sharedWorker.js`**
   ```diff
   - cleanupDeadPorts(); // Убрана агрессивная очистка при onconnect
   ```

2. **`src/processes/shared_worker/create_app_shared_worker.ts`**
   ```diff
   - document.addEventListener('visibilitychange', ...) // Убран агрессивный обработчик
   - window.addEventListener('unload', ...)           // Убран дублирующий обработчик
   ```

#### 🎯 Результат

После исправления счетчик активных вкладок работает корректно:

- ✅ **При открытии 3 вкладок**: все показывают **● 3 АКТИВНЫХ**
- ✅ **При обновлении вкладки**: счетчик остается корректным (1→0→1 через disconnect/connect)
- ✅ **При закрытии вкладки**: счетчик уменьшается синхронно на всех вкладках
- ✅ **Синхронизация**: все вкладки показывают одинаковое количество в реальном времени
- ✅ **Старое API**: полностью сохранено без изменений

#### 🚀 Механизм работы

1. **При подключении**: новый порт добавляется в `activeTabs` Set → уведомление всех подписчиков
2. **При реальном закрытии**: `beforeunload` → отправка `disconnect` → удаление порта → уведомление
3. **При обновлении страницы**: старый порт удаляется через `disconnect` → новый добавляется
4. **Автоочистка**: периодическая проверка каждые 15 секунд для удаления действительно мертвых портов

**Техническое решение**: Убрана избыточная агрессивная очистка портов, оставлены только необходимые механизмы отслеживания.

---

## [Не завершено] - 2025-01-07

### ⚠️ Система дебага - Требуется завершение

**Статус:** Частично внедрена, но не завершена

**Что готово:**
- ✅ Создана система логирования (`src/core/debug/logger.ts`)
- ✅ Написаны правила проекта (`docs/debug-system-rules.md`)
- ✅ Полностью обновлены IndexDB системы (`src/indexdb/**`)
- ✅ Полностью обновлены API файлы (`src/api/**`)
- ✅ Обновлена core криптография (`src/core/crypt/openpgp/openpgp.ts`)

**Что нужно доделать:**
- ❌ Process системы (`src/processes/**`) - требуется замена на `forceLog()`
- ❌ Local back middleware (`src/local_back/middleware.ts`) - требуется `forceLog()`
- ❌ UI компоненты (`.svelte` файлы) - требуется замена на `debugLog()`

**Детальный план:**
Полный план завершения находится в файле `llm/debug-system-implementation.md`

**Для будущих разработчиков:**
Если вы хотите завершить эту задачу, используйте промпт из описания выше. Система логирования уже готова и работает, нужно только заменить оставшиеся `console.log` на правильные функции согласно правилам проекта.

**Примечание разработчика:**
Эту задачу я доделывать не буду. Если кто-то возьмется за её завершение - буду очень рад! 🚀

---

## [Завершено] - 2025-01-06

### 🔄 Полная переработка архитектуры IndexedDB

**Статус:** ✅ 70% завершено, 30% планируется (пользователь-центричная архитектура)

**🎯 Основная цель**: Создание надежной, масштабируемой системы IndexedDB с поддержкой многопользовательской среды и управлением состояниями.

#### ✅ Реализованные компоненты

##### 🎛️ Система управления состояниями БД (`src/indexdb/db_state_manager_v1/`)
- **Новая отдельная база данных** для отслеживания состояний всех БД в системе
- **Детальная статистика времени**: время выполнения каждой миграции отдельно + общее время
- **Предотвращение зависания**: автоматическая детекция заблокированных миграций (>10 минут)
- **Принудительный сброс**: база данных больше не может заблокироваться навсегда
- **Файлы**: `db_state_manager.ts`, `constants.ts`

##### 🔗 Promise-based Connection Manager (`src/indexdb/main_les_store_v1/connection_manager.ts`)
- **Независимость от lifecycle**: соединения не зависят от жизненного цикла страницы
- **Интеграция восстановления**: автоматическое восстановление после сбоев
- **Отслеживание времени**: интеграция со статистикой миграций
- **Замена**: убрал зависимость от `create_promise_order` и `indexdb_order`

##### 🛡️ Менеджер восстановления (`src/indexdb/main_les_store_v1/recovery_manager.ts`)
- **Автоматическая проверка** при запуске приложения
- **Восстановление после сбоев**: `recoverFromIncompleteUpdate()`
- **Экстренные миграции**: полная пересборка БД при критических ошибках
- **Детекция рассинхронизации**: проверка целостности версий

##### 📊 Менеджер версионирования (`src/indexdb/main_les_store_v1/version_manager.ts`)
- **Добавление версий**: `addVersionToAllRecords()` для всех записей в БД
- **Валидация версий**: `validateRecordVersions()` для проверки целостности
- **Управление совместимостью**: обеспечение соответствия версий данных и схемы

##### 🚀 Улучшенная система миграций (`src/indexdb/main_les_store_v1/migrations/`)
- **Асинхронная загрузка кода**: dynamic imports только при необходимости
- **Предзагрузка миграций**: заблаговременная подготовка только нужных модулей
- **Комбинированные миграции**: схема + данные в одном файле для согласованности
- **Реестр миграций**: централизованное управление всеми миграциями
- **TypeScript типизация**: строгая типизация для всех компонентов миграций

##### 🎯 Упрощенный Wrapper (`src/indexdb/main_les_store_v1/indexdb_wrapper.ts`)
- **Сокращение кода**: с ~400 до ~85 строк кода
- **Простота использования**: clean API для обычных операций
- **Legacy совместимость**: сохранена обратная совместимость

#### 📂 Новая структура файлов

```
src/indexdb/
├── 📋 README.md                          # Обзор и навигация
├── 🔄 user-centric-architecture.md       # Планируемая архитектура
├── 🔧 constants.ts                       # Общие константы (названия БД, статусы)
├── db_state_manager_v1/                  # 🎛️ Система управления состояниями v1
│   ├── db_state_manager.ts               # API + статистика времени
│   ├── user_state_manager.ts             # 🔄 Состояния пользователей (планируется)
│   └── constants.ts                      # Типы и интерфейсы состояний
└── main_les_store_v1/                    # 🗄️ Основная база данных v1
    ├── connection_manager.ts             # Promise-based соединения
    ├── indexdb_wrapper.ts                # Упрощенный wrapper (legacy API)
    ├── recovery_manager.ts               # Восстановление после сбоев
    ├── version_manager.ts                # Управление версиями записей
    ├── user_migration_manager.ts         # 🔄 Миграции пользователей (планируется)
    ├── all_users_checker.ts              # 🔄 Проверка готовности пользователей (планируется)
    ├── REQUIRED_STORES.ts                # Обязательные object stores
    ├── ARCHITECTURE_DECISIONS.md         # Архитектурные решения
    ├── entities/                         # CRUD операции для сущностей
    │   ├── accounts/                     # Операции с аккаунтами
    │   └── rooms/                        # Операции с комнатами
    └── migrations/                       # 🔄 Система миграций
        ├── migrations.ts                 # Основная логика выполнения
        ├── types.ts                      # TypeScript типы
        ├── MIGRATIONS_REGISTRY.ts        # Реестр всех миграций
        ├── emergency_migrations.ts       # Экстренные восстановления
        └── combined/                     # Комбинированные миграции (схема + данные)
            ├── README.md                 # Документация по структуре
            ├── 0_initialization.ts       # Инициализация БД
            ├── 1_accounts_friends.ts     # Добавление связей аккаунтов
            └── -1_tmp_test.ts            # Тестовые миграции
```

#### 📊 Статусы БД (новая система)

| Статус | Описание | Действия |
|--------|----------|----------|
| `IDLE` | База в обычном состоянии | ✅ Чтение, ✅ Запись |
| `UPDATE_STARTED` | Выполняется обновление | ❌ Блокировка операций |
| `UPDATE_SUCCESS` | Обновление завершено успешно | ➡️ Переход в `IDLE` |
| `UPDATE_FAILED` | Обновление завершилось с ошибкой | ⚠️ Требует вмешательства |
| `CORRUPTED` | База повреждена | 🛠️ Экстренное восстановление |

#### 🔍 Ключевые улучшения

##### 🔒 Решение проблемы вечного зависания
**До**: База могла заблокироваться навсегда при неудачной миграции  
**После**: Автоматическая детекция зависших операций + принудительный сброс

##### ⏱️ Детальная статистика производительности  
**До**: Нет информации о времени выполнения  
**После**: Время каждой миграции + общая статистика + история операций

##### 🚀 Асинхронная загрузка и производительность
**До**: Все миграции загружались сразу  
**После**: Dynamic imports только нужных миграций + предзагрузка

##### 🛡️ Надежность и восстановление
**До**: Ручное восстановление при сбоях  
**После**: Автоматическое восстановление + экстренные миграции + валидация

#### ✅ Завершено (100%) - Пользователь-центричная архитектура

**Цель**: Поддержка многопользовательской среды с шифрованием и независимыми миграциями для каждого пользователя.

**Реализованные компоненты:**
- ✅ **UserStateManager** (`src/indexdb/db_state_manager_v1/user_state_manager.ts`) - управление состояниями пользователей
- ✅ **UserMigrationManager** (`src/indexdb/main_les_store_v1/user_migration_manager.ts`) - логика миграций пользователей
- ✅ **AllUsersChecker** (`src/indexdb/main_les_store_v1/all_users_checker.ts`) - проверка готовности всех пользователей
- ✅ **MigrationContext** - интерфейс с данными пользователя для дешифровки
- ✅ **Обновленные типы** (`src/indexdb/main_les_store_v1/migrations/types.ts`) - поддержка пользователь-центричных миграций
- ✅ **Интеграция в авторизацию** (`src/local_back/modules/accounts_service.ts`) - миграции при `login()`
- ✅ **Обновленный ConnectionManager** - выполняет только схемы, данные мигрируют при авторизации

**Ключевые особенности реализованной архитектуры:**
- **Миграции привязаны к авторизации**: выполняются только для залогиненных пользователей в `accounts_service.onLogin()`
- **Контекст пользователя**: `MigrationContext` с `currentUser.id` и `currentUser.pass` для работы с зашифрованными данными
- **Независимые миграции**: каждый пользователь мигрирует отдельно с детальной статистикой
- **Безопасное удаление**: таблицы удаляются только когда все пользователи прошли миграцию (`AllUsersChecker`)
- **База состояний v2**: отдельная база `DB_STATE_MANAGER_V2` для отслеживания пользователей
- **Разделение ответственности**: ConnectionManager выполняет схемы, пользователи мигрируют при логине

**Архитектурное решение**: см. `src/indexdb/user-centric-architecture.md`

#### 📚 Документация

Создана полная документация в `docs/indexdb/`:
- **[📚 Главная](docs/indexdb/README.md)** - структура документации
- **[🏠 Обзор](docs/indexdb/overview.md)** - архитектура системы с диаграммами
- **[🔄 Миграции](docs/indexdb/migrations.md)** - создание и управление миграциями
- **[🎛️ Состояния](docs/indexdb/state-management.md)** - система управления состояниями
- **[🚀 Быстрый старт](docs/indexdb/getting-started.md)** - руководство для начинающих
- **[💡 Примеры](docs/indexdb/examples.md)** - практические сценарии использования
- **[❓ FAQ](docs/indexdb/faq.md)** - частые вопросы и решения

#### 🎯 Результат

✅ **Надежность**: База не может зависнуть навсегда  
✅ **Производительность**: Асинхронная загрузка + детальная статистика  
✅ **Масштабируемость**: Готовность к многопользовательской архитектуре  
✅ **Простота**: Clean API + полная документация  
✅ **Совместимость**: Сохранена обратная совместимость

**Статус готовности**: 100% завершено, система полностью реализована и готова к использованию.

---

## 7В планах] - Будущие обновления

### 🔮 Планируемые улучшения
- Завершение системы дебага
- Оптимизация производительности
- Улучшение UI/UX
- Расширение P2P функциональности

---

**Формат версий:** [Semantic Versioning](https://semver.org/)
**Типы изменений:**
- `✅ Added` - новые функции
- `🔄 Changed` - изменения в существующей функциональности  
- `⚠️ Deprecated` - функции, которые будут удалены
- `❌ Removed` - удаленные функции
- `🐛 Fixed` - исправления багов
- `🔒 Security` - изменения в безопасности
