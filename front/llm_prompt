# Контекстный файл проекта secure-message

## 🚀 Быстрая справка (читать всегда):
**Q: Фреймворк?** A: Svelte 5 + TypeScript  
**Q: Компоненты?** A: `src/components/` - UI, `src/widgets/` - бизнес-логика  
**Q: Стили?** A: Tailwind + `rem` единицы  
**Q: Архитектура?** A: `llm/architecture.md`  

## Справочные файлы (по необходимости):
- **llm/rules.md** - 9 обязательных правил
- **llm/quick-reference.md** - минимальный контекст 
- **llm/svelte5.md** - Svelte 5 синтаксис
- **llm/libp2p.md** - P2P сеть (НЕ устанавливать без нужды!)
- **llm/testing.md** - тесты

## ⚡ Svelte 5 кратко:
```svelte
let count = $state(0);
let double = $derived(count * 2);
$effect(() => console.log(count));
```

## ❌ Частые ошибки:
- `src/components/widgets/` - НЕТ! Только `src/widgets/`
- `px` единицы - НЕТ! Только `rem`
- Английский язык - НЕТ! Только русский

**ГЛАВНОЕ**: Всегда читай `llm/rules.md` и `llm/architecture.md` перед работой!
