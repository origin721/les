services:
  les-scripton:
    build:
      context: ..
      dockerfile: docker/Dockerfile
    container_name: les-scripton-development
    ports:
      - "8080:8080"
    volumes:
      - ../:/app
    working_dir: /app
    stdin_open: true
    tty: true
    command: sh
